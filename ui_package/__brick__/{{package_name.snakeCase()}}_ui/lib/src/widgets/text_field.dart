import 'package:flutter/material.dart';

class {{package_name.pascalCase()}}TextField extends StatelessWidget {
  const {{package_name.pascalCase()}}TextField({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const TextField();
  }
}
