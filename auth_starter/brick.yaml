name: auth_starter
description: A new brick created with the Mason CLI.
version: 0.1.0+1
environment:
  mason: ">=0.1.0-dev.51 <0.1.0"

vars:
  add_dependencies:
    type: boolean
    description: Whether or not add flutter_bloc & equatable
    default: false
    prompt: Add flutter_bloc & equatable?
