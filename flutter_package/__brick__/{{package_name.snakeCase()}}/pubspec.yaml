name: {{package_name.snakeCase()}}
description: {{package_name.pascalCase()}} pure dart package.
version: 0.0.1

environment: 
  sdk: ">=2.16.0 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  build_runner: ^2.0.3
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0