name: dart_package
description: A generic package template

version: 0.2.0

environment:
  mason: ^0.1.1

vars:
  package_name:
    type: string
    description: Package name
    default: users repository
    prompt: Package name
